---
import Layout from '../../layouts/Layout.astro';
import FreeTrialBooking from '../../components/booking/FreeTrialBooking';

const focus = Astro.url.searchParams.get('focus') || '';
const user = Astro.locals.user;
const userProp = user
  ? {
      id: user.id,
      email: user.email ?? '',
      displayName: (user.user_metadata?.display_name as string) || user.email || '',
    }
  : null;
---

<Layout title="ライフコーチング初回無料体験">
  <section class="py-12 px-4">
    <div class="max-w-3xl mx-auto">
      {/* Hero */}
      <div class="text-center mb-10">
        <h1 class="text-2xl md:text-3xl font-bold text-gray-800 mb-6">
          「焦ってるのに動けない」を、<br class="hidden sm:inline" />60分で見える化する。
        </h1>

        <div class="bg-white rounded-2xl shadow-sm border border-orange-100 p-6 md:p-8 text-left space-y-4 text-gray-600 text-sm md:text-base leading-relaxed">
          <p>まずは一度、話してみませんか？</p>
          <p>
            この無料セッションでは、ライフバランス診断であなたの現在地を一緒に確認し、<br class="hidden sm:inline" />
            「何が自分を止めてるか」を見える化します。見えたら、次に何をすべきかも見えてくる。
          </p>
          <ul class="space-y-2 text-gray-700">
            <li class="flex items-center gap-2">
              <span class="text-orange-500">⏱</span> 所要時間：60分
            </li>
            <li class="flex items-center gap-2">
              <span class="text-orange-500">💻</span> 形式：オンライン（Zoom）
            </li>
            <li class="flex items-center gap-2">
              <span class="text-orange-500">🆓</span> 料金：無料
            </li>
            <li class="flex items-center gap-2">
              <span class="text-orange-500">👤</span> 定員：各回1名<span class="text-gray-400 text-xs ml-1">（※今後グループセッションも予定）</span>
            </li>
          </ul>
        </div>
      </div>

      {/* Booking flow */}
      <div class="mb-4 text-center">
        <h2 class="text-lg font-bold text-gray-700">ご希望の日時をお選びください</h2>
      </div>

      <FreeTrialBooking client:load focusParam={focus} user={userProp} />
    </div>
  </section>
</Layout>
